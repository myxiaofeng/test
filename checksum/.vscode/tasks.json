{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make md5",
            "type": "shell",
            //"command": "cd build && cmake ../src -DCMAKE_BUILD_TYPE=Debug .. && make ",
            "command": "cd build && cmake ../src  .. && make ",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}